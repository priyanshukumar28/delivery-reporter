DATABASE_URL="postgresql://postgres:postgres@localhost:5432/delivery_reporter?schema=public"
JWT_SECRET="change-this-to-a-long-random-string"
PORT=4000
# In production (Render), set to this service's own public URL — used to build report imageUrl links.
PUBLIC_URL="http://localhost:4000"
# In production (Render), set to your deployed Vercel frontend URL, no trailing slash.
CORS_ORIGIN="http://localhost:5173"
