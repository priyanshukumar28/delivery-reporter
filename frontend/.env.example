# Optional for local dev (vite.config.js already proxies /api to localhost:4000).
# Required in production (Vercel) — set to your deployed Render backend URL, no trailing slash.
VITE_API_URL="https://your-backend.onrender.com"
